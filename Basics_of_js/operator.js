  /* 
    1.arithmatic operator

     + ,-,*,/

    2.Assignment operator
    =

    3.Comparision Operator
    >,<,>=,<=,==,!=,===,!==

    4.Logical operator
    &&, ||, !
    ex->>
    i.&&
    (first && second)-->{
      if first is false it does not check for the second 
    }
    ii.||
    (first || second)-->{
      if first is true it does not check for the second 
    }

   5.Bitwise operator
   >> bitwise right shift,<< bitwise left shift, >>> bitwise unsigned right shift

*/

// ex of >>> 
const a = 5;
const b = 2;
const c = -5;
console.log(a >>> b);//1
console.log(c >>> b);//-->1073741822

//condinal (terniry) operator
//condition ? ifTrue:ifFalse;
